"use client";

// StoreCleaner is intentionally disabled.
// All user data (accounts, vehicles, rides, expenses etc.) persists in localStorage.
// To manually clear data during development, open browser DevTools → Application → Local Storage → clear manually.

export default function StoreCleaner() {
  return null;
}
